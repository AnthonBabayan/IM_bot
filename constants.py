# Состояния диалога
SELECT_TYPE, SW_SUBMENU, HW_SUBMENU, SUMMARY, PHOTO_UPLOAD, CONFIRM = range(6)
AUTH_EMAIL, AUTH_CODE = range(10, 12)  # новые состояния для авторизации
# --- Новые состояния для оценки ---
EVALUATE, FEEDBACK = range(20, 22) 